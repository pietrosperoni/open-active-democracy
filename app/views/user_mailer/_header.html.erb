<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <table>
      <tr>
        <td width="130px" align="left">
          <% if @government.layout.include?("better_reykjavik") %>
              <% if defined?(attachments)%>
                <%= image_tag attachments['BR_logo.png'].url %>
              <% else %>
                <%= image_tag "logos/BR_email.png" %>
              <% end %>
          <% else %>
              <% if defined?(attachments)%>
                <%= image_tag attachments['logo.png'].url %>
              <% else %>
                <%= image_tag "logos/email.png" %>
              <% end %>
          <% end %>
        </td>
        <td>
        <h1 style="margin-left: 15px;font-family:sans-serif,helvetica,arial"><%=tr("Your Priorities","email")%>
        </td>
     </tr>
   </table>
