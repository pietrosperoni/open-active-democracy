<% if priority.replaced? %>
	<div id="hp_intro"><strike><%= h(priority.name)%></strike></div>
<% else %>
	<div id="hp_intro" class="priorities"><%= link_to_unless_current h(priority.name), priority %> (<%=priority.cached_allocated_points_counter%>)</div>
<% end %>