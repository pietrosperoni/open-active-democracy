<% unless session[:have_shown_welcome] or Government.current.layout=="better_reykjavik" or Government.current.layout=="better_iceland" %>
    <div id="encouragements"><%= render :partial => "ads/welcome"%></div>
<% else %>
  <% if logged_in? %>  
  	<% ad = current_user.pick_ad(current_priority_ids) %>
  	<% if ad %>
  	 <div id="encouragements"><div id="notification_show">
  		<%= render :partial => "ads/show", :locals => {:ad => ad }%>
  	</div></div>
  	<% end %>
  <% else %>
    <% ad = Ad.active.filtered.by_random.first %>
    <% if ad %>
     <div id="encouragements"><div id="notification_show">
      <%= render :partial => "ads/show", :locals => {:ad => ad }%>
    </div></div>
    <% end %>
  <% end %>
<% end %>
