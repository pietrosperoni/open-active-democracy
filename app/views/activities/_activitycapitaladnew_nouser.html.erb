<img src="/images/icons/coins_delete.png" height="16" width="16" border="0" class="icon">
<% if not activity.ad %>
<%= raw tr("Spent {capital}{currency_short_name} on an <a href='{ad_url}'>ad</a> for {priority_name}", "view/activities/_activitycapitaladnew_nouser", :user_name => link_to(activity.user.login, activity.user), :capital => activity.capital.amount.abs, :currency_short_name => current_government.currency_short_name, :priority_name => link_to(activity.priority.name, activity.priority), :ad_url => url_for(activity.priority))%>
<% else %>
<%= raw tr("Spent {capital}{currency_short_name} on an <a href='{ad_url}'>ad</a> for {priority_name}", "view/activities/_activitycapitaladnew_nouser", :user_name => link_to(activity.user.login, activity.user), :capital => activity.capital.amount.abs, :currency_short_name => current_government.currency_short_name, :priority_name => link_to(activity.priority.name, activity.priority), :ad_url => priority_ad_url(activity.priority,activity.ad))%>
<% end %>