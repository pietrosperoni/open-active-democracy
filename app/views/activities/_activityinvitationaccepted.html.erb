<img src="/images/icons/email_open_image.png" height="16" width="16" border="0" class="icon">
<% if activity.other_user %>
	<%= raw tr("{user_name} accepted an invitation from {other_user_name} to join {government_name}", "view/activities/_activityinvitationaccepted", :user_name => link_to(activity.user.login, activity.user), :other_user_name => link_to(activity.other_user.login, activity.other_user), :government_name => tr(current_government.name,"Name from database"))%>
<% else %>
	<%= raw tr("{user_name} accepted an invitation to join {government_name}", "view/activities/_activityinvitationaccepted", :user_name => link_to(activity.user.login, activity.user), :government_name => tr(current_government.name,"Name from database"))%>
<% end %>
