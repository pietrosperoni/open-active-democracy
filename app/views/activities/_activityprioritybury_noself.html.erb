<img src="/images/icons/cancel.png" height="16" width="16" border="0" class="icon">
<%= raw tr("{user_name} buried this priority. It will no longer show up in the rankings. It's probably obvious why.", "view/activities/_activityprioritybury_noself", :user_name => link_to(activity.user.login, activity.user), :priority_name => link_to(activity.priority.name, activity.priority)) %>