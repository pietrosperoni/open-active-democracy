<div id="hp_intro"><%= @page_title %></div>

<%= form_for :password, :url => passwords_path do |f| -%>
<p><label for="email"><%= tr("Your email address", "view/passwords/new")%></label><br/>
<%= text_field_tag :email %> <%= submit_tag tr("Request a new password", "view/passwords/new"), :class => "yellow_button" %></p>
<% end -%>

<%= link_to tr("Never mind, I remember it now", "view/passwords/new"), new_session_url %>