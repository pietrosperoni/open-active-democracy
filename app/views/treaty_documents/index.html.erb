<table>
	<tr>
		<td>
		</td>
		<td colspan="10"><img src="/images/esb_treaty.png" alt=" " width="414" height="102" /></td> 
		<!-- 
		<%# TreatyDocument::TREATY_ARRAY[0][:negotiation_stages].each do |stage| %>
		<td>
			<%#=stage[:name]%>
		</td>-->
		<% #end %>
	</tr>

<% Tag.by_external_id.each do |tag| %>
	<tr>	
		<td class="td_Underline">
			<a href="#" onclick="showTreatyDocumentChapterWindow('<%=tag.external_id%>')"><%=tag.external_id%>. <%=tag.name%></a>
		</td>
		<% TreatyDocument::NEGOTIATION_STAGES.each do |stage| %>			
			<% count = TreatyDocument.tagged_with(make_quoted(tag.name),:match_all=>:true).by_negotiation_status(stage[:id]).count %>
			<% if count > 0 and count < TreatyDocument::MAXIMUM_NUMBER_OF_DOCUMENTS %>
				<td class="merkt" style="background-color:<%= GrayScaleColor.from_fraction(1-(count.to_f/TreatyDocument::MAXIMUM_NUMBER_OF_DOCUMENTS.to_f)).html %>">
			<% elsif count > 0 %>
				<td class="files_max">
			<% else %>
				<td class="merkt">
				<span style="color:#fff">
			<% end %>
				<% if count>0 %>
					<a href="#" onclick="showTreatyDocumentWindow(<%=tag.external_id%>,<%=stage[:id]%>)"><%=count%></a>
				<% else %>
					<%=count%>
				<% end %>
			</span>
		</td>
		<% end %>
		 <td class="tom">&nbsp;</td> 
	</tr>
<% end %>
</table> 
